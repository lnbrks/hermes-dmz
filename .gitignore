# Secrets — never commit
.env
**/.env
/deploy/state/
/host/gws_secrets/

# Private email data + v1 runtime outputs — never commit
/manual-pass/
/agent/runs/
/agent/state/

# Python
__pycache__/
*.pyc

# Hermes runtime artifacts written into the bind-mounted skills dir
/hermes/skills/.usage.json
/hermes/skills/.usage.json.lock
